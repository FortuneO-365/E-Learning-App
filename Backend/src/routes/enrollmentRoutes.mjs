import { Router } from "express";

const router = Router();

router.get('/api/enrollments', (req, res) => {}) // Get my enrollments

router.post('/api/enroll/:courseId', (req, res) => {}) // Enroll in a course